version: '3'

services:
  # Ubuntu
  ubuntu:
    build:
      context: ./
      dockerfile: "${DEV_PATH}/ubuntu/Dockerfile"

  # Application php8.0 and symfony
  app:
    container_name: app
    build:
      context: ./
      dockerfile: "${DEV_PATH}/php${PHP_VERSION}-fpm/Dockerfile"
      args:
        workdir: ${WORKDIR}
        app_path: ${APP_PATH}
    volumes:
      - "${APP_PATH}:${WORKDIR}"
      - "${DEV_PATH}/php8.0-fpm/php.ini:/usr/local/etc/php/conf.d/php.ini"
    links:
      - ubuntu
      - mysql
      - redis

  # Database MySQL
  mysql:
    container_name: mysql
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${MYSQL_USER_PASSWORD}"
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_USER_PASSWORD}"
      MYSQL_DATABASE: "${MYSQL_DBNAME}"
    ports:
      - "3306:3306"
    volumes:
      - "${DATA_PATH}/mysql:/var/lib/mysql"

  # Database No SQL Redis
  redis:
    container_name: redis
    image: redis:6.2.6-alpine
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - "${DEV_PATH}/redis/default.conf:/etc/redis/conf.d/redis.conf"

  # Web-Server nginx
  nginx:
    container_name: nginx
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "${DEV_PATH}/nginx/default.conf:/etc/nginx/conf.d/default.conf"
      - "${APP_PATH}:${WORKDIR}"
    links:
      - app