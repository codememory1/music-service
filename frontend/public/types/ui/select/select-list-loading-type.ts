import SelectOptionType from '~/types/ui/select/select-option-type';

type SelectListLoadingType = {
  isLoading: boolean;
  options: Array<SelectOptionType>;
};

export default SelectListLoadingType;
