type CalendarDateType = {
  year?: number;
  month?: number;
  day?: number;
};

type CalendarPickType = {
  day: number;
};

type CalendarPickSteep = {
  id: string;
};

export { CalendarDateType, CalendarPickType, CalendarPickSteep };
